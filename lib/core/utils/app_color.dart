import 'package:flutter/material.dart';

class AppColor {
  // light mode
  static Color bgLight = Color(0xffFFFFFF);

  // dark mode
  static Color bgDark = Color(0xff171717);

  // common
  static Color grayColor = Color(0xff808080);
  static Color transparent = Colors.transparent;
}
